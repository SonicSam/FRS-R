- error = @release.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @release.errors.include?(:filesize)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :filesize, :class => 'control-label'
  .controls
    =f.text_field :filesize, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:filesize, :class => 'text-error') : pat(:example)
- error = @release.errors.include?(:numfiles)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :numfiles, :class => 'control-label'
  .controls
    =f.text_field :numfiles, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:numfiles, :class => 'text-error') : pat(:example)
- error = @release.errors.include?(:account_ids)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :account_ids, :class => 'control-label'
  .controls
    =f.text_field :account_ids, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:account_ids, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:releases, :index), :class => 'btn btn-default'